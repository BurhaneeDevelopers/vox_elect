/**
 * Individual conversation page — /chat/[id]
 * Same UI as /chat but loads specific conversation
 */

import { ChatPageClient } from '@/components/chat/chat_page_client';

export default function ConversationPage() {
  return <ChatPageClient />;
}
